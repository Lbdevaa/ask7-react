import React from 'react';
import './index.sass';

function PortfolioPageContainer(PropTypes) {
  return <div className="portfolio-page-container">{PropTypes.children}</div>;
}

export default PortfolioPageContainer;
