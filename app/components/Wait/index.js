import React from 'react';
import './wait.sass';

function Wait() {
  return (
    <div className="wait" id="preloader">
      <span className="wait__button" />
      <span className="wait__button" />
      <span className="wait__button" />
    </div>
  );
}

export default Wait;
