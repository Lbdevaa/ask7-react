import styled from 'styled-components';

const List = styled.ul`
  padding-left: 0;
  margin: 0;
  list-style: none;
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  grid-gap: 30px;
`;

export default List;
