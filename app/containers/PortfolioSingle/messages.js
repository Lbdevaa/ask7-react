/*
 * HomePage Messages
 *
 * This contains all the text for the PortfolioSingle container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PortfolioSingle';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'УралПолиКом',
  },
});
